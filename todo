- implement response streaming [WIP]
- organize api code into dif mod [WIP]
- Find way to only create tokio runtime once [WIP]

- something that lets user know chat bot is thinking (while waiting for answers)
- try writing it out word by word / char by char to simulate claude
- dark theme ?
- Change style to look more like a chat app
- make chat row not take up full width if text content doesn't need full width
- when incoming chat arrives, scroll window down to accomodate it?
- allow cmd+v paste / cmd+c copy / word select
- Allow user to enter own api key / handle persistence